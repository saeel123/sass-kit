1.in home 
sudo gem install sass

2.cd css/0-plugins 0-plugins dir
sudo gem install bourbon
  bourbon install 
sudo gem install neat
  neat install 
sudo gem install bitters
  bitters install

3.in css folder
  sass --watch app.sass:app.css

4.riffils must be added to the 2-modules dir
